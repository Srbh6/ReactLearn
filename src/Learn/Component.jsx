// Q.1 What is component
//pice of code that can reuse
// such as function
// but more powerful then a function
// header footer is best example

// Q.2 Why Use component

// Q.3 because we don't want to write code multiple time

// Q.4 Type of component

// Two type of component
// 1. functional component

// Syntax for functional cmp -
function App() {
  return <h1> Hello this is functional cmp </h1>;
}

//  2. class component     //is more powerful
// When creating a React component, the component's name must start with an upper case letter.
// // The component has to include the [[extends React.Component]] statement, this statement creates an inheritance to React.Component, and gives your component access to React.Component's functions.
// The component also requires a render() method, this method returns HTML.

// syntax class cmp-

//  Class Mobile extends React.Cop {
//  render() {
//  return <h1> Hi this is syntax of class cmp </h1>;
//  }
//  }

// Create a component

// Use a component
